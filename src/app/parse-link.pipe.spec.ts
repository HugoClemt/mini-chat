import { ParseLinkPipe } from './parse-link.pipe';

describe('ParseLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
